name: Apply Home 1 Why USS mosaic redesign

on:
  push:
    branches: [main]
    paths:
      - 'scripts/redesign_home1_why_v2.py'
      - '.github/workflows/run-home1-why-v2.yml'

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Apply Home 1 Why USS redesign
        run: python3 scripts/redesign_home1_why_v2.py
      - name: Commit Home 1 changes
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add home1.html
          if git diff --cached --quiet; then
            echo "No changes required"
            exit 0
          fi
          git commit -m "Redesign Home 1 Why USS section"
          git push
